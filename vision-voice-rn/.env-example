# Variáveis EXPO_PUBLIC_* são validadas em src/env.ts (Zod).
# Copie para .env e preencha os valores.

EXPO_PUBLIC_API_DETECTION_URL=
EXPO_PUBLIC_API_CAPTION_URL=
EXPO_PUBLIC_API_TRANSCRIPTION_URL=
EXPO_PUBLIC_API_TRANSCRIPTION_KEY=
EXPO_PUBLIC_API_TRANSLATION_URL=
EXPO_PUBLIC_API_TRANSLATION_KEY=
EXPO_PUBLIC_API_TRANSCRIPTION_KEY=

# --- Build para produção ---
#
# Build LOCAL (npx expo prebuild / expo run:ios|android):
#   - Use .env ou .env.local com os valores de produção.
#   - Esses arquivos são lidos na hora do build. Mantenha .env no .gitignore.
#
# EAS Build (eas build):
#   - Configure as variáveis no EAS (dashboard ou CLI), por ambiente:
#       eas env:create --name EXPO_PUBLIC_API_DETECTION_URL --value "https://..." --environment production --visibility plainText
#       eas env:create --name EXPO_PUBLIC_API_CAPTION_URL --value "https://..." --environment production --visibility plainText
#       eas env:create --name EXPO_PUBLIC_API_TRANSCRIPTION_URL --value "https://..." --environment production --visibility plainText
#   - Repita para "preview" e "development" se precisar de valores diferentes.
#   - O eas.json já mapeia cada perfil (development/preview/production) para o environment correspondente.
#   - Para puxar as variáveis do EAS para uso local: eas env:pull --environment production
